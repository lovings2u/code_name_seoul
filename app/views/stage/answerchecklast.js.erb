if ("<%= @message %>" == "P") {
  swal({
    title: '총 플레이 시간',
    text: '<%= @lasttime  %>',
    type: 'success',
    confirmButtonText: '확인',
    confirmButtonColor: '#38e798'
  }).then(function() {
    
  }, function(dismiss){
    window.location.reload();
  })
} else {
  swal({
    title: '틀렸습니다.',
    text: '어렵다면 힌트를 사용해보세요!',
    type: 'error',
    confirmButtonText: '확인',
    confirmButtonColor: '#38e798'
  }).then(function() {
  })
}
