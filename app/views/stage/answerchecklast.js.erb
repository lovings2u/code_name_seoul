if ("<%= @message %>" == "P") {
  swal({
    title: '총 플레이 시간',
    text: '<%= @lasttime  %>',
    type: 'success',
    confirmButtonText: '확인',
    confirmButtonColor: '#38e798'
  }).then(function() {

  }, function(dismiss){
    window.location.reload();
  })
} else {
  swal({
    title: '틀렸습니다.',
    type: 'error',
    confirmButtonText: '확인',
    confirmButtonColor: '#38e798'
  }).then(function() {
  })
}
