<div class="short" style="margin-top: -900px;">
  <%= form_tag '/answer', id: 'form', class: "text-center", remote: true, :onsubmit => "checkanswer2()" do %>
    <div class="question">
      <input type="text" required/>
      <input type="text" required/>
      <input type="text" required/>
    </div>
    <h1>한 글자씩 입력하세요</h1>
    <%= hidden_field_tag 'answer' %>
    <%= submit_tag '정답 입력', id: "getBtn" %>
  <% end %>
</div>
